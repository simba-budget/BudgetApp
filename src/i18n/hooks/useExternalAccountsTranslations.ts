import { useTranslation } from 'react-i18next';

export const useExternalAccountsTranslations = () => {
  return useTranslation('externalAccounts');
};

export default useExternalAccountsTranslations;
